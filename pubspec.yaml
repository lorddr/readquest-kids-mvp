name: readquest_kids_mvp
description: "ReadQuest Kids - App gamificată pentru citit fluent cu Bubo"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.3.4
  firebase_auth: ^5.3.1

  # UI & UX
  cached_network_image: ^3.4.1
  lottie: ^3.1.2

  # Speech Recognition
  speech_to_text: ^7.0.0

  # Utils
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/illustrations/
    - assets/lottie/